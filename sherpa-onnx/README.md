# KeVoiceInput Modified sherpa-onnx

这是为 KeVoiceInput 项目专门定制修改的 sherpa-onnx 库，增加对 SeACo Paraformer 模型的 `model_eb` 支持。

## 自定义功能
- 添加对 SeACo Paraformer 的 `model_eb.onnx` 支持
- C/C++/Python API 中添加 `model_eb` 功能
- 专为 KeVoiceInput 热词检测优化

## 注意
此仓库只包含 KeVoiceInput 项目实际需要的自定义修改，保持最小体积。

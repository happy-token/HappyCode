# HappyCode TODOs

Generated by /plan-ceo-review on 2026-05-01

## P0 — Do immediately (blocking everything else)

- [ ] `git init` in this repo — three months of build with no version control is unacceptable
- [ ] Update `CLAUDE.md`: change "当前应该执行：Phase 0" to reflect Phase 1/2 actual state — every AI session reading this file will be confused

## W0 — Before any feature work

- [ ] Confirm Apple Developer account is active — required for W7-8 notarization; enroll now to avoid a hard block at distribution

## W1-4 — Phase 1 polish (new acceptance criteria)

- [ ] Startup <3s cold (measure before and after polish sprint)
- [ ] Subagent tree: stable to 50 nodes, no layout breaks, no memory leak
- [ ] Add `process.on('uncaughtException')` → append to local log file in `app.getPath('logs')` — crash reporting for beta users
- [ ] Relay pre-flight ping in `agent-manager.ts`: before calling `query()`, HEAD or GET `/v1/models` on the configured baseUrl; on failure, show toast naming the relay URL and linking to provider settings (adds ~200ms on success, saves session-start confusion on failure)

## W2-4 — TokenUsage integration

- [ ] NSOpenPanel + security-scoped bookmark for cross-app SQLite read (required for notarized builds; "auto-detect" is not viable in sandbox)
- [ ] Add entitlement: `com.apple.security.files.user-selected.read-only`
- [ ] Render `cacheHitRate` as "N/A" when null (backward compat with older TokenUsage versions)
- [ ] Stale data indicator: show timestamp of last successful read when DB is locked or bookmark revoked

## W3-6 — IM integration

- [ ] WeChat: implement per P2 spike result (WxPusher token OR 企业微信 webhook — not WeChatFerry which is Windows-only)
- [ ] Feishu: incoming webhook adapter
- [ ] Telegram: bot token + chatId adapter
- [ ] Credentials stored via `electron.safeStorage` — test storage behavior on unsigned dev builds before W7 signing to confirm no crash on transition
- [ ] Auto-reconnect: 3 retries with exponential backoff on connection drop
- [ ] Send failure: surface error in toast; do not crash session
- [ ] Markdown unsupported: fall back to plain text silently

## W4-6 — MCP discovery gallery

- [ ] "Discover MCP Servers" tab in MCP management screen
- [ ] Curated JSON file in repo: minimum 10 entries (name, description, GitHub URL, category)
- [ ] Install button calls `saveMcpServer()` from `mcp-config.ts` — NOT `installSkillFromGit()` from `skills-manager.ts` (these are separate concepts)
- [ ] Install success: toast; install error: shown inline

## W5-6 — Integration tests

- [ ] vitest setup in main process
- [ ] Test: IM adapter send path (mock network)
- [ ] Test: TokenUsage DB read (test fixture database)
- [ ] Test: Relay pre-flight ping (mock fetch)

## W7 — Monthly spend report via IM

- [ ] IM message template: total cost (USD), top project, session count, date range; Markdown for Feishu/Telegram, plain text for WeChat
- [ ] If IM disconnected at report time: toast error + skip report (do not queue)

## W7-8 — Distribution

- [ ] Mac notarization via `@electron/notarize` in CI (entitlements: hardened runtime + `com.apple.security.files.user-selected.read-only`)
- [ ] Sparkle auto-update: wire `SUFeedURL` to platform website appcast
- [ ] GitHub Releases: automated from CI on tag push
- [ ] Confirm `safeStorage` credentials survive signing transition (test explicitly)
